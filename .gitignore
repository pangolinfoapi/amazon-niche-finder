__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.env

# NOTE: niches.json, data/ and reports/ are intentionally committed —
# they are your public opportunity dataset, and the GitHub Actions workflow
# needs niches.json + the SQLite history to compute trends.
